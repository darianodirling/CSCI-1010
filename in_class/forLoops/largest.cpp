#include<iostream>
using namespace std;

int main()
{
    int i, a = 0, largest =;

    cout << "Number: " << endl;

    for (i = 0; i <= 10; i++)
    {
        if(a > largest) //compare first
        {
          largest = a;
        } cin >> a; //enter new numbers
    }
    cout << "Largest: " << largest << endl;
}